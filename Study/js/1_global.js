// syntax parser - read our code and check whether all syntax are valid or not

// execution context -
// javascript code is wrapped using javascript engine and runs in execution context

//The global execution context is created by javascript engine before any code is executed.
//Whenever a function is executed, a new execution context gets created.

// anything outside the function is attached to global object

// default global object is windows

// this == global objet

